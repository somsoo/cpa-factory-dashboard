# 🏭 Factory Standard Operating Procedure (SOP) - 팩토리 통합 매뉴얼 (v2.0)

이 문서는 자동화 블로그 및 사이트(CPA, 쿠팡, 경제, 스레드, 원페이지 등)를 생성하고 유지보수할 때 AI 에이전트가 **반드시** 준수해야 하는 최상위 시스템 매뉴얼입니다. 단순한 구조 가이드를 넘어 마케팅, SEO, 인프라, 광고주 페널티 방지 규정까지 포함된 절대 원칙입니다.

---

## 1. 🏷️ 메타데이터 및 대시보드 식별 (Metadata & Tracking)
- **명찰 의무화:** 모든 리포지토리 루트에는 반드시 `.factory.json` 파일이 존재해야 함.
- **포맷:** `{"type": "<cpa|coupang|economy|threads|onepage>", "name": "<저장소명>", "domain": "<접속URL>"}`
- **목적:** 중앙 대시보드(`cpa-factory-dashboard`)가 GitHub API를 통해 실시간으로 팩토리 현황을 100% 추적하도록 보장.

## 2. 🧠 AI 환각 방지 및 시스템 구조 (Anti-Hallucination)
- **Frontmatter 절대 금지:** 제미나이(AI)는 절대 YAML Frontmatter (`--- layout: post ... ---`)를 출력해서는 안 됨. 오직 Markdown 본문만 출력.
- **파이썬 조립:** 포스팅 파일 생성을 위한 날짜, 카테고리, 제목 등 메타데이터와 YAML 헤더는 오직 파이썬 스크립트(`auto_poster.py`) 단에서 결합하여 생성함.
- **2-Pass 검열 로직:** AI는 한 번에 글을 쓰지 않음. 반드시 1차 초안(Draft) 작성 후, 2차 검열(금지어, SEO, 구조 확인)을 거쳐 최종본을 내보내는 '2-Pass 로직'으로 스크립트를 구성할 것.

## 3. 📈 데이터 기반 리얼 SEO (Data-Driven SEO)
- **CPA 블로그 (정밀 타겟팅):** `auto_poster.py` 실행 시 AI 뇌피셜이 아닌, **'네이버 모바일 자동완성 API'**를 호출하여 해당 캠페인의 '실제 롱테일 키워드 상위 4개'를 추출하고 프롬프트에 주입.
- **경제/쿠팡 블로그 (주제 발굴):** 구글 트렌드 일간 RSS API (`geo=KR` 또는 `geo=US`)를 호출하여 실시간 핫이슈를 추출.
- **제목 작성 규칙:** 40~60자 사이로 작성. 추출된 트렌드/롱테일 키워드를 반드시 앞부분에 배치하고, 단순 광고가 아닌 '유용한 꿀팁 정보글' 느낌으로 CTR을 극대화.

## 4. ⚠️ 광고주 페널티 및 허위 사실 유포 방지 (Compliance & Safety)
- **엄격한 팩트 체크:** 광고주가 수수료를 환수(Clawback)하는 사태를 막기 위해 다음 사항을 프롬프트에 강력히 통제함.
  - **엠베스트/엘리하이:** 무조건 '7일 무료 체험'임 (10일이라고 거짓말 금지). '스마트 기기/태블릿 무상 증정' 같은 허위 과장 광고 절대 금지.
  - **USIM 스마트인증:** 완전히 무료가 아님. '가입 후 3개월 지나면 월 990원 부과' 팩트를 명시할 것.
  - **타겟 매칭:** B2B 창업/소상공인 캠페인을 맘카페나 학생 타겟으로 잘못 포장하지 말 것.

## 5. 🎨 UI/UX 및 수익화 레이아웃 (Monetization & Layout)
- **이미지 규격:** 본문에 삽입되는 모든 이미지는 CSS를 통해 `max-width: 80% !important; margin: 0 auto; display: block; border-radius: 8px;` 적용. (모바일 가독성)
- **수익화 버튼 (CTA):** 
  - CPA: 글 중간(해결책 제시 직후)과 하단에 눈에 띄는 색상의 CTA 버튼을 최소 2회 삽입.
  - 쿠팡: 하단에 반드시 "이 포스팅은 쿠팡 파트너스 활동의 일환으로..." 대가성 문구 기재.
- **템플릿 정화:** 지킬 테마의 `_config.yml`이나 `footer.html`에 포함된 가짜 이메일, 깃허브 프로필 링크, 안 쓰는 Contact 폼 삭제.
- **네비게이션:** 독자 이탈 방지를 위해 `_layouts/post.html` 하단에 **"📋 전체 글 목록 보기"** 버튼 하드코딩.

## 6. ⚙️ 인프라 및 Git 충돌 방지 룰 (Infrastructure & Git)
- **Git Rebase 필수:** Github Actions(원격 봇)가 주기적으로 글을 푸시하고 있으므로, 로컬에서 코드를 수정 후 푸시할 때는 **무조건 `git config pull.rebase true && git pull origin main`**을 선행하여 충돌(Rejected)을 방지할 것.
- **Cloudflare 연동:** 새 사이트 생성 시 환경변수의 `CLOUDFLARE_API_TOKEN`과 Zone ID를 활용해 서브도메인을 자동으로 연결할 것.
- **인코딩 안정성:** 파이썬 파일 입출력 시 윈도우 환경을 고려해 반드시 `encoding='utf-8'`을 명시하고, JSON 파싱 시 UTF-8 BOM(`\ufeff`) 오류 처리를 꼼꼼히 할 것.
- **명령어 환경:** Windows 환경이므로 Unix용 `cat << EOF` 대신 안전한 스크립트 작성/실행 툴을 활용할 것.

---
> **[ AI Agent Command ]** 
> 이 매뉴얼은 팩토리의 헌법이다. 사용자의 추가 지시가 없더라도, 새로운 작업을 시작할 때 위 6대 분야(메타데이터, 구조, SEO, 광고주 규정, 디자인, 인프라)의 규칙을 100% 엄격하게 적용하라.
