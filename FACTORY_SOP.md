# 🏭 Factory Standard Operating Procedure (SOP)

이 문서는 자동화 블로그 및 사이트(CPA, 쿠팡, 경제, 스레드, 원페이지 등)를 생성하고 유지보수할 때 AI 에이전트가 **반드시** 준수해야 하는 최상위 시스템 매뉴얼입니다.

## 1. 🏷️ 메타데이터 식별 (Metadata & Tracking)
- **규칙:** 모든 공장(리포지토리)의 최상단 루트 디렉토리에는 반드시 `.factory.json` 파일이 존재해야 한다.
- **포맷:** `{"type": "<유형>", "name": "<저장소명>", "domain": "<접속URL>"}`
- **유형(type) 값:** `cpa`, `coupang`, `economy`, `threads`, `onepage` 중 하나를 사용.
- **목적:** 대시보드 스캐너가 이 파일만 보고 공장 현황을 실시간으로 추적할 수 있도록 보장함.

## 2. 🧠 AI 프롬프트 및 시스템 구조 (Anti-Hallucination)
- **Frontmatter 절대 금지:** 제미나이(AI)에게 본문을 생성하라고 지시할 때, 절대 YAML Frontmatter (`--- layout: post ... ---`)를 출력하지 못하게 프롬프트로 강제한다.
- **파이썬 조립:** 포스팅 파일 생성을 위한 날짜, 카테고리, 제목 등 메타데이터와 YAML 헤더는 오직 파이썬 스크립트(`auto_poster.py`) 단에서 문자열 연산으로 결합하여 생성한다.
- **정제:** AI가 실수로 YAML을 출력하더라도 정규식(`re.sub`)을 통해 본문에서 완전히 제거한 후 저장한다.

## 3. 📈 리얼 데이터 기반 SEO (Search Engine Optimization)
- **CPA 블로그:** `auto_poster.py` 실행 시 AI 뇌피셜이 아닌, **'네이버 모바일 자동완성 API'**를 호출하여 해당 캠페인의 '실제 롱테일 키워드 3~5개'를 먼저 추출하고 이를 프롬프트에 주입한다.
- **경제/쿠팡 블로그:** 구글 트렌드 일간 RSS API (`geo=KR` 또는 `geo=US`)를 호출하여 실시간 트렌드 키워드 상위권을 추출한 뒤 프롬프트에 주입한다.
- **제목 작성:** 40~60자 사이로 작성하되, 트렌드/롱테일 키워드를 앞부분에 배치하고 단순 광고가 아닌 '유용한 정보글(꿀팁)' 느낌으로 CTR(클릭률)을 극대화한다.

## 4. 🎨 UI/UX 및 수익화 레이아웃 (Monetization Layout)
- **이미지 규격:** 본문에 삽입되는 모든 이미지(Unsplash 등)는 CSS를 통해 `max-width: 80% !important; margin: 0 auto; display: block; border-radius: 8px;` 속성이 적용되어야 한다.
- **수익화 버튼 (CTA):** 
  - CPA: 글 중간과 하단에 눈에 띄는 색상(예: #ff5722)의 둥근 모서리 CTA 버튼을 최소 2회 삽입한다.
  - 쿠팡: 버튼 하단에 반드시 "이 포스팅은 쿠팡 파트너스 활동의 일환으로..." 문구를 작은 글씨로 삽입한다.
- **불필요한 템플릿 제거:** 지킬 테마의 `_config.yml`이나 `footer.html`에 포함된 쓰레기 데이터(가짜 이메일, 깃허브 프로필 링크, 안 안 쓰는 Contact 폼)는 모두 삭제한다.
- **네비게이션:** 독자의 체류 시간을 늘리기 위해 `_layouts/post.html` 하단(본문 끝)에 **"📋 전체 글 목록 보기"** 버튼을 하드코딩하여 메인(`/`)으로 돌아갈 수 있게 조치한다.

---
> **AI Agent Instruction:** 
> 이 매뉴얼을 읽은 즉시, 사용자의 추가 지시가 없더라도 위 규칙들을 현재 작업 중인 코드와 시스템에 100% 엄격하게 적용하여 실행할 것.
